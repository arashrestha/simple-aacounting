

<table style="width:60%">
  <tr>
    <th>Data</th>
    <th>Title</th>
    <th>Amount</th>
    <th></th>
    <th></th>
  </tr>

  <% @sims.each do |sim| %>
  			<tr>
 				<td><%= sim.date %></td>
	 			<td><%= sim.title %></td>
				<td><%= sim.amount %></td>
				<td><%= link_to 'Edit', edit_sim_path(sim), class: 'btn btn-primary'%></td>
				<td><%= link_to 'Delete', sim_path(sim), method: :delete, class: 'btn btn-danger', data: {confirm: "Are you sure?"} %></td>
	 		</tr>
	
<% end %>

</table>



